export function PageFooter() {
  return (
    <footer className="text-sm text-[var(--muted)]">
      &copy; {new Date().getFullYear()} Cultivate Focus. Mindful progress, one
      session at a time.
    </footer>
  );
}
